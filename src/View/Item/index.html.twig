{% extends 'layout.html.twig' %}

{% block content %}
    {% for post in posts %}
        <article>
            <header>{{ post.pseudo }}</header>
            <img src="{{ post.photo }}" alt="Photo de {{ post.pseudo }}">
            <footer>{{ user.story }}</footer>
        </article>
    {% endfor %}
{% endblock %}

